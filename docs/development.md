# راهنمای توسعه

<div dir="rtl">

## 🛠️ راه‌اندازی محیط توسعه

### 1. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 2. تنظیم متغیرهای محیطی

فایل `.env` را ایجاد کنید:

```env
DATABASE_URL=postgresql://user:password@localhost/sede_db
REDIS_URL=redis://localhost:6379
OPENAI_API_KEY=your_key_here
```

### 3. راه‌اندازی پایگاه داده

```bash
python scripts/init_db.py
```

## 📝 استانداردهای کدنویسی

- استفاده از **PEP 8** برای Python
- مستندسازی کامل با **docstrings**
- استفاده از **Type Hints**
- نوشتن **Unit Tests** برای هر ماژول

## 🧪 اجرای تست‌ها

```bash
pytest tests/ -v --cov=src
```

## 📦 ساختار پروژه

```
src/
├── chatbot/          # ماژول چت‌بات
│   ├── __init__.py
│   ├── engine.py     # موتور اصلی چت‌بات
│   ├── nlu.py        # پردازش زبان طبیعی
│   └── query_gen.py  # تولید کوئری
├── crawler/          # ماژول خزنده
│   ├── __init__.py
│   ├── base.py       # کلاس پایه خزنده
│   ├── divar.py      # خزنده دیوار
│   └── ...
└── ...
```

## 🔄 Git Workflow

1. ایجاد Branch جدید برای Feature
2. Commit با پیام‌های واضح
3. Push و ایجاد Pull Request
4. Review و Merge

</div>

