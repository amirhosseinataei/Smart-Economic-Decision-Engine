# معماری سامانه SEDE

<div dir="rtl">

## 📐 نمای کلی معماری

سامانه SEDE بر اساس معماری میکروسرویس و الگوی سه‌لایه طراحی شده است:

```
┌─────────────────────────────────────────────────────────┐
│                    Presentation Layer                    │
│  (Web Interface, Mobile App, API Gateway)                │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────┐
│                    Business Logic Layer                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Chatbot    │  │   Crawler    │  │   Analyzer   │  │
│  │   Engine     │  │   Engine     │  │   Engine     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────┐
│                      Data Layer                          │
│  (PostgreSQL, Redis, File Storage, External APIs)        │
└─────────────────────────────────────────────────────────┘
```

## 🔄 جریان داده (Data Flow)

### 1. جریان درخواست کاربر

```
User Input → Chatbot Engine → Intent Recognition → Entity Extraction
    ↓
Query Generator → Crawler Engine → Multiple Websites
    ↓
Data Collection → Fraud Detection → Economic Analysis
    ↓
Recommendation Engine → Response Generation → User
```

### 2. معماری چت‌بات

```
┌──────────────────────────────────────────────┐
│           User Input (Natural Language)      │
└──────────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼───────┐      ┌────────▼────────┐
│  LLM Parser   │      │  Regex Parser   │
│  (Fallback)   │      │  (Primary)      │
└───────┬───────┘      └────────┬────────┘
        │                       │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │  Intent Classifier    │
        │  Entity Extractor     │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │  Structured JSON      │
        │  (User Query Schema)  │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │  Economic Solver      │
        │  (Budget Calculator)  │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │  Query Generator      │
        └───────────────────────┘
```

### 3. معماری خزنده

```
┌──────────────────────────────────────────────┐
│         Query Generator (Multiple Queries)    │
└──────────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼───────┐      ┌────────▼────────┐
│  Divar       │      │  Sheypoor       │
│  Crawler     │      │  Crawler        │
└───────┬───────┘      └────────┬────────┘
        │                       │
┌───────▼───────┐      ┌────────▼────────┐
│  Bama         │      │  Torob          │
│  Crawler      │      │  Crawler        │
└───────┬───────┘      └────────┬────────┘
        │                       │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │  Data Normalizer      │
        │  (Unified Schema)     │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │  JSON Output          │
        │  (Products/Ads)       │
        └───────────────────────┘
```

## 🧩 ماژول‌های اصلی

### 1. Chatbot Engine
- **Intent Recognition**: تشخیص هدف کاربر
- **Entity Extraction**: استخراج موجودیت‌ها (بودجه، مکان، نوع محصول)
- **Context Management**: مدیریت مکالمه
- **Query Generation**: تولید کوئری‌های ساختاریافته

### 2. Crawler Engine
- **Multi-Site Support**: پشتیبانی از چندین سایت
- **Pattern Matching**: تطبیق الگوهای مختلف HTML
- **Rate Limiting**: کنترل سرعت درخواست
- **Data Extraction**: استخراج داده‌های ساختاریافته

### 3. Analysis Engine
- **Fraud Detection**: تشخیص تخلفات
- **Economic Analysis**: تحلیل اقتصادی
- **Recommendation**: پیشنهاد بهترین گزینه‌ها

## 🔒 امنیت

- **Encryption**: رمزنگاری داده‌های حساس
- **Authentication**: احراز هویت کاربران
- **Rate Limiting**: محدودیت درخواست
- **Input Validation**: اعتبارسنجی ورودی‌ها

## 📊 مقیاس‌پذیری

- **Microservices**: معماری میکروسرویس
- **Caching**: استفاده از Redis
- **Load Balancing**: توزیع بار
- **Async Processing**: پردازش غیرهمزمان

</div>

